# CLAUDE.md

## 一条规矩，和它为什么必须是机械的

**花算力之前先读产生这个值的代码。**

这条规矩在 `AGENTS.md §5.5` 里已经写过一次，**是我自己写的，然后我自己违反了**：
2026-08-30 发现 engine 类目标 665 条通过率精确 0%，我提出下游补丁并**启动了 1386
条 SAM3 实验**；根因就在 `models/object_extraction_model.py` 的 SYSTEM_PROMPT 一行里
（`a running engine` 被明写为合格示例）。独立派出的盲测 agent 拿到同一份数据，
**也提出了同类下游补丁，也没读那个 prompt**。打断这件事的是用户，不是任何检查。

所以：**不要指望读到这段文字就能避免。产出压力下，能立刻跑出数字的方案永远赢。**
真正起作用的是下面两道机械闸门。

## 两道闸门（都在 avgraph-strategy-lab 仓库）

**启动前** —— `python3 loop/preflight.py <declare.json>`
凡是要花 GPU 或 API 额度的实验，先过这一关。它拦：大开销却没定位根因（R1）、
根因已知却仍在下游修且说不出理由（R2）、改变任务语义却没有**独立**复核（R3）。
不过就不许启动。

**记录前** —— `python3 loop/step.py <step.json>`
它拦：比率没配绝对数（C1）、结论没标 定理/实测/猜想（C2）、提修法没记根因哪一行（C3）、
没回答「改完还是不是原来那个任务」（C4）、声称省算力没扣沉没成本（C6）、
从数据反推阈值却只核了吻合的那一侧（C7）。
不过就不写 LOG、不 commit。

两道闸门的七条 + 三条检查，**每一条都对应一次真实失误**，不是假想清单。
C7 是 2026-08-30 加的：我从「边界 0.15 与阈值吻合」推出首帧闸 = 0.1，
只核了吻合那侧；实际 (0.05, 0.10] 有 611 条非零值，闸是 0.05。
单侧吻合能被多个候选阈值同时满足，判别力为 0。
细节看 `loop/checks.py` 和 `loop/preflight.py` 的顶部注释。

## 三件不许动的东西

gate 阈值、外生质量线、验收口径。理由和实测代价见 `AGENTS.md §1`
（动了：−21.4% 交付、污染率 62%、上报产量虚高 2.6–3.5 倍）。

正确表述是「阈值由离线带标注整定，agent 不得触碰」，**不是**「阈值天生神圣」——
现有证据说当前的 `τ=0.55` 本身就没整定对。

## 一条 agent 复核替代不了的东西

R3 允许 agent 做语义复核，但记住 2026-08-30 的记录：**独立 agent 犯了和我同样的错**。
多 agent 买到的是**去相关的错误**（对抗性复核确实找到 4 个真 bug），
**不是去相关的价值观**——所有 agent 都偏向"优化可测的指标"，没人天然会问
"这么改之后还是不是原来那个任务"。

**涉及 correctness 约束的改动（如音视频必须移除同一对象），等人类确认。**

## 现状速查

- 基线：8,907 条真实运行，h₀ = 21.84%（95% CI 20.99–22.71）。
  **但这 8,907 条全部跑在放宽档**：实测首帧闸 = **0.05**（3,432 条非零值里
  ≤0.05 的精确为 0 条，下界 0.0501），而 `SAM3_FIRST_FRAME_INTENDED = 0.80`，
  差 16 倍。注意 `nodes.py` 的当前默认是 0.1，与历史运行的 0.05 又不同——
  0.05 从哪来尚未查明（env 覆盖？更早的代码版本？），待查。
  按预期值 0.80 回算，1,945 条交付里只剩 45 条（−97.7%）；0.30 档剩 1,011 条。
  该回算只数真实比例 >0.80 的样本，与当时闸设在哪无关，故不受上述不确定性影响。
  **h₀ 描述的是 0.05 这一档，不是预期配置**，别拿它当预期档的基线用。
  待核：首帧比例的分母是**整帧面积**（`largest_cc_ratio`），要求 >0.80 等于
  要求物体盖住画面八成——预期值可能本意是「最大连通域 / mask 总面积」。
  参考脚本 `filter_first_frame.py` 在服务器上，核对前不许动阈值。
- 瓶颈：mask gate 条件通过率 28.71%，其中 58.3% 的 mask **精确为 0**。
  零值是 SAM3 的真实输出，不是抹除假象——零值组独有的高频词是
  `vehicle engine` 140 条、`truck engine` 84 条、`footsteps` 40 条，全不可分割。
- 音频 gate 是空操作：8,907 条里只拒了 1 条。
  **原因不是闸太松，是分数根本没测**：`audio_removal_check` 只有一行
  `state.get("mock_audio_removal_score", 0.90)`，从不调用任何模型。
  到达该闸的 1,946 条里 `audio_removal_score` 只有 2 个取值（1,945 条恰为 0.90），
  而 `visual_removal_score` 有 693 个。**音频侧从未被验证过，一条都没有。**
- 五个待测量（h₀ σ Δq κ A）已测 h₀ 与 A，σ 在主瓶颈上实测 **= 0**（SAM3 确定性）
- **命题 1「别做逐样本搜索」的地位是「有条件」，不是「确认」**——它随闸档翻转：

  | 首帧闸 | 交付 | h₀ | 95%CI | 判定 |
  |---|---|---|---|---|
  | 0.05（历史实跑） | 1,945 | 21.84% | [20.99, 22.71] | 别搜索（稳） |
  | 0.30 | 1,011 | 11.35% | [10.71, 12.03] | 别搜索（稳） |
  | 0.50 | 269 | 3.02% | [2.68, 3.40] | 落在分界带内·不可判 |
  | 0.80（代码预期） | 45 | 0.51% | [0.38, 0.68] | **搜索（稳）** |

  分界带 1%–5%（随 gamma 平移，中心 3.5%）。「稳」= 整个 CI 落在带的同一侧。
  不等式 `搜索赢 ⟺ h₁/h₀ > κ` 本身没被推翻，被推翻的是把某个 h₀ 数值
  当成该 pipeline 的固有属性。**记录任何率，必须同时记录产生它的配置档位。**
- 任何 ≤0.15 的首帧闸都是**空转**的：1,945 条交付样本的 `mask_area_ratio`
  下界是 0.1501，真正生效的一直是 `routes.py` 的 `MASK_AREA_THRESHOLD = 0.15`。
  「开发档 vs 预期档」的差别只在 0.15 以上才开始体现。
- **另两条裁决也需修订**（2026-08-30 重审，三条裁决没有一条能原样保留）：
  - 命题 3「阈值放大 2–163 倍」原判**否定**，依据是阈值附近密度 A 只有
    10.6%/1.7%/0.0%。但音频那项（0/1946 条）是拿常数 0.90 算的，**空证据**；
    mask 那项分母含 4,789 条被抹成 0、无论阈值怎么挪都过不了的样本，
    排除后 A 从 873/8221 = 10.6% 变为 **873/3432 = 25.4%**。
    A 还随闸档单调降：0.15→25.4%、0.30→19.9%、0.50→6.0%、0.80→1.1%。
    降为**有条件**，不是改判——A 只是放大倍数的代理量，不能直接换算。
  - 「K 道 gate 不对称」原判**否定**，方向对但理由要改写：不是音频闸太松，
    是分数从未测量。改写后该裁决更强——两道闸无法比较，因为其中一道不是闸。
- **口径规矩**：算「阈值附近密度」时，分母只能取结局可能被该阈值改变的样本；
  被上游抹除或已定死的样本必须排除，否则会系统性低估阈值的影响力。
- 日志缺口：`sounding_objects` 与 caption **都没落盘**（后者丢了约 1,484 GPU-小时）
